const fn = () =>{
    const miString = "esto es un string dybbuk"
    let b = miString + " se estrenará ep + página"
    console.log(b)
}

fn()

const clikeando = () => {
    let a = "y luego disco"

    console.log(a)
}